# VEN-INV-PROTECTED: Protected Vendor Invoices (NEW CATEGORY)

**Status:** TODO

## Goal

New sub-category for vendor invoices from "protected" vendors that require special handling compared to regular VEN-INV.

## Details

_To be defined — based on email samples._

## Open Questions

- Which vendors are "protected" and what triggers the classification?
- What processing differs from standard VEN-INV? (e.g., no auto-forward, no draft reply, different folder?)
- Should this be a separate category or a flag within VEN-INV?
- Folder routing: where do processed emails end up?

## Sample Emails

Place sample `.eml` or `.msg` files in `dev_tasks/emails/` (gitignored).
